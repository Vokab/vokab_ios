import IMAGES from './images';
import {
  COLORS,
  SIZES,
  FONTS,
  SHADOW,
  COLORS_THEME,
  FONT_TEXT_CUSTOM1,
} from './theme';
import icons from './icons';

export {IMAGES, COLORS, SIZES, FONTS, icons, COLORS_THEME, FONT_TEXT_CUSTOM1};
